# RBX Manager

## Installation

To install RBX Manager in editable mode, run the following command:

```bash
pip install -e .
```

## Quick Start

Instead of using `python main.py`, you can now use the `rbx-m` command as a shortcut.

Simply run:

```bash
rbx-m
```

This will execute the RBX Manager application with the desired settings and configurations.